# 2026-03-14

## 创建 EdgeOne Pages 部署 Skill
- 参考 vercel-labs/agent-skills 的 deploy-to-vercel skill 结构
- 创建了完整的 skill 目录结构：
  - `skills/deploy-to-edgeone/SKILL.md` - 核心指令文件，定义部署流程
  - `skills/deploy-to-edgeone/resources/deploy.sh` - 自动化部署脚本
  - `README.md` - 项目说明和安装指南
  - `package.json` - 项目元数据
- 部署流程：检测 CLI → 检测认证（支持 browser login 和 API Token 两种方式）→ 构建 → 部署
- EdgeOne CLI 文档参考：https://cloud.tencent.com/document/product/1552/127423
- GitHub 仓库：https://github.com/xiaban-x/deploy-to-edgeone-pages

## 根据实际部署反馈优化 Skill
基于实际部署测试发现的两个问题，更新了 SKILL.md 和 deploy.sh：
1. **远程构建超时问题**：EdgeOne 远程构建时 `mirrors.tencent.com` 连接超时
   - SKILL.md Step 3 重写为"优先本地构建"策略，先 `npm run build` 再部署产物
   - deploy.sh 添加超时错误检测，自动回退到本地构建再重试部署
2. **项目数量超限（40个上限）**：`Pages project exceeds 40 limit`
   - SKILL.md Troubleshooting 添加完整处理流程：
     - Option 1: 引导用户去控制台 https://edgeone.ai/pages 手动删除
     - Option 2: 用户提供 api_token + project_id，通过 DeletePagesProject API 删除
   - deploy.sh 添加 `--delete` 模式，可通过脚本调用 API 删除项目
   - deploy.sh 部署失败时自动检测错误类型并给出针对性建议

## 第三轮优化（3个问题修复）
1. **API Token 控制台链接**：所有提到 API Token 的地方都加上了创建链接
   - 国内站: https://console.cloud.tencent.com/edgeone/pages?tab=settings
   - 国际站: https://console.tencentcloud.com/edgeone/pages?tab=settings
2. **部署成功输出完整 URL**：
   - 要求 AI 展示完整的 `EDGEONE_DEPLOY_URL`（带 eo_token 和 eo_time 参数）
   - 提醒预览 URL 有效期只有 3 小时
   - 引导用户去控制台绑定自定义域名获取永久 URL
3. **修正部署流程**：
   - 旧流程：先本地 `npm run build` 再部署产物
   - 新流程：先 `edgeone pages deploy`（远程构建）→ 失败则 `edgeone pages build`（本地构建）→ `edgeone pages deploy .edgeone`
   - deploy.sh 同步修正为新流程

## 第四轮优化（强化 AI 遵循度）
发现第三轮的修改虽然文件已更新到位（`/Users/xiaban/.codebuddy/skills/deploy-to-edgeone/`），但 AI 仍未严格遵循 SKILL.md 指令（URL 被截断、部署流程未改变）。本轮进行了以下强化：
1. **文档顶部添加 🚨 TOP-LEVEL RULES**：三条最关键的规则放在最显眼的位置
2. **Step 1 精简**：移除了 `ls -d .next dist build` 等检查构建产物的步骤，避免 AI 被误导去直接部署已有的 `.next`
3. **Path A 重写**：移除 `[path]` 参数，添加显著警告「不要手动构建或查找构建产物」
4. **Step 3 添加 🚨 MANDATORY FLOW 警告**：明确禁止跳过或重排步骤
5. **Output 部分重构**：拆分为三条独立规则（Rule 1/2/3），并用 WRONG ❌ vs CORRECT ✅ 对比示例
6. **同步更新**：确认 `/Users/xiaban/.codebuddy/skills/deploy-to-edgeone/` 已安装版本与源码完全一致
